programa

declare int a, int b, int c , int d.

escreva("Programa Teste").
escreva("Digite A").
leia(a).

escreva("Digite B").
leia (b).

se(a<b) entao 
	c := a+b.
senao entao 
	c:=a-b.

escreva("C e igual a ").
escreva(c).

d := c*a+b.

escreva("D e igual a").
escreva(d).

faca {
	a := a + b.
	d := d - 1.
} enquanto (d > 0)

enquanto (a > 0) {
	a := a - 1.
}

fimprog.
