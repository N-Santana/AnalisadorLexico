programa 
/* codigo que exemplifica a utilizacao correta da estrura condicional se entao senao entao. */

declare numeric p1, numeric p2, numeric soma, numeric media.

escreva("Digite a nota da p1:").
leia(p1).
//p1 := 5,3.

escreva("Digite a nota da p2:").
leia(p2).

soma := p1 + p2.

media := soma/2.

escreva("A media das provas eh:").
escreva(media). 

se (media > 5 ) entao {
    escreva("O Aluno foi aprovado").
} senao {
    escreva("O Aluno foi reprovado").
}

fimprog.
